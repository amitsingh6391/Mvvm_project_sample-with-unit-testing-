name: mvvm_project_sample
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

# dependencies:
#   flutter:
#     sdk: flutter
#   cupertino_icons: ^1.0.2
# dev_dependencies:
#   flutter_test:
#     sdk: flutter
#   flutter_lints: ^1.0.0
dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_screenutil: ^5.0.0+2
  google_fonts: ^2.1.0
  http: ^0.13.4
  lottie: ^1.2.1
  provider: ^6.0.1
  shared_preferences: ^2.0.8
  confetti: ^0.6.0


dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
